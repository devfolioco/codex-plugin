# Privacy Policy

This plugin stores no data and sends no data by itself.

The bundled helper script only reads local JSON files unless you explicitly run the `preview` command with a preview endpoint. In that case, the script sends the selected submission JSON to the endpoint you provide.

When you use Devfolio, Devfolio's own terms and privacy policy apply.
